name: _orphan (temp)
on: { workflow_dispatch: {} }
env: { APP_URL: "https://influencer-seeding-mu.vercel.app" }
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl -sS "$APP_URL/api/b2b-revenue/fetch?orphan=1&dump=1" -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
          echo
