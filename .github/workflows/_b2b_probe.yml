name: _b2b probe (temp)

# 임시: 제품별 통일 fetch 호출 → 응답 확인. 확인 후 삭제.
on:
  workflow_dispatch:

env:
  APP_URL: "https://influencer-seeding-mu.vercel.app"

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: b2b fetch 호출
        run: |
          curl -sS "$APP_URL/api/b2b-revenue/fetch" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
          echo
