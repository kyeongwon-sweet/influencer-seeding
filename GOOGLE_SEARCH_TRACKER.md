# 구글 검색 트래킹

- 메뉴/주소: `/google-search-tracker`. 기존 Vercel Next.js 서버와 Clerk 로그인을 사용한다.
- 상품 1~5개, 상품당 검색어 1~25개. 묶음 내 검색어는 `+`(OR), 상품 묶음 간은 `,`로 한 Google Trends 비교 요청을 만든다. 개별 요청의 지수를 더하지 않는다.
- 국가: KR/US/JP/전 세계. 분석 전 28일을 포함해 수집·정규화한다. 이 범위의 최대 관심도가 100이다. 다른 기간의 저장된 결과와 높이를 직접 비교하지 않는다.
- 실제 검색 횟수, 누적 검색 횟수, 연령별 지표는 제공하지 않는다. `hasData=false`는 null, 실제로 반환된 유효 0은 0이다. 미완료 구간은 제외한다. 주·월 데이터는 보간하지 않는다.
- 급등: 직전 28일 유효 표본 최소 3개 평균 대비 설정 배수 이상 + 최소 지수 이상. 설정 일수 안의 급등은 하나로 묶고, 3개 이상 표본에 걸쳐 7일 이상 이어지면 지속 상승이다. 상승률은 피크 표본의 직전 평균 기준이며 평균 0이면 계산하지 않는다. 원본 Streamlit 비공개 내부 공식과 동일하다고 주장하지 않는다.
- 수동 날짜는 실제 관측이 없으면 공란으로 표시하며 수치를 만들지 않는다.
- 콘텐츠: YouTube 대표 검색어 인기 결과 중 실제 게시일 ±기간을 확인. 인스타는 상품별 태그 결과를 이벤트 전체에서 재사용. 네이버 뉴스·블로그는 최신 각 100건에서 기간 확인. 완전한 과거 콘텐츠 목록이나 당시 인기순위가 아니다.
- 저장은 계정별 **현재 브라우저 localStorage**에 최대 8개. 팀 공유/서버 저장이 아니다. 같은 이름은 갱신한다. DB·시트에 쓰지 않는다.
- 수집은 버튼 요청만 실행. 기존 `APIFY_API_TOKEN`, `WEBHOOK_SECRET`(없으면 Clerk secret) 사용. 실행 수명 600초, 검색 수집 최대 요금 $1/회, YT/IG 각각 $2/회. 액터별 최근 실행을 확인해 진행 중이면 새 실행을 거절한다(분산 원자적 락은 아님).
- 실행 receipt는 HMAC 서명, Clerk 사용자 소유권, 24시간 만료 확인. 토큰을 브라우저에 전달하지 않는다. 페이지 재방문 시 저장된 실행의 완료 확인을 이어간다.
- XLSX: 분석 기준·검색 관심도·상품 요약·이벤트·조회 콘텐츠 5시트. CSV와 PNG 지원. Cowork 프롬프트는 선택한 이벤트와 수집 완료된 콘텐츠로 생성·복사한다.
- 기존 `/api/google-trends/collect`, webhook 및 `google_search_trends` 테이블과 분리해 기존 정기 수집·정규화 결과를 변경하지 않는다.

근거: [Trends 지표](https://support.google.com/trends/answer/4365533), [검색어 묶음](https://support.google.com/trends/answer/4359550), [Apify Google Trends](https://apify.com/apify/google-trends-scraper/input-schema).
